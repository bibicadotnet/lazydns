# Installation & Updates

## From source
```bash
git clone <repo>
cargo build --release
```

## Cross-compilation
Notes on `cross_build.sh` and common targets.

## Docker images
How to build and run the provided Docker images (see `docker/` folder).

## Upgrading
Guidance on upgrading between versions and reading the changelog.

---

TODO: Add platform-specific steps and recommended build flags.